// ============================================================
// SETTINGS — MODULE PLACEHOLDER
// Stub: theme, shortcuts, data dir, backup, per-module toggles,
// notification preferences. See phase3 audit doc for full spec.
// ============================================================

export default function Settings() {
  return (
    <div style={{ padding: "2rem", color: "var(--muted-foreground)" }}>
      <h2>Settings</h2>
      <p>Settings module — implementation pending (Phase 4).</p>
    </div>
  );
}
