// ============================================================
// GOALS — MODULE PLACEHOLDER
// Stub: Goals → Milestones → Tasks hierarchy.
// Goal progress auto-calculated from linked task completion.
// Weekly goal review integration with journal.
// See phase3 audit doc for full spec.
// ============================================================

export default function Goals() {
  return (
    <div style={{ padding: "2rem", color: "var(--muted-foreground)" }}>
      <h2>Goals</h2>
      <p>Goals module — implementation pending.</p>
    </div>
  );
}
